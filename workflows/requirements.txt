# Render Workflows SDK
render-sdk>=0.1.0

# Database
asyncpg>=0.29.0

# HTTP Client
aiohttp>=3.9.0
requests>=2.31.0  # For auth_setup.py token verification

# OAuth & Encryption
oauthlib>=3.2.0  # Core OAuth2 protocol logic
requests-oauthlib>=1.3.0  # OAuth2 with requests (for auth_setup.py)
cryptography>=42.0.0  # Token encryption (Fernet)

# YAML parsing for render.yaml
PyYAML>=6.0.1

# Environment variables
python-dotenv>=1.0.0

# Timezone support
pytz>=2024.1
